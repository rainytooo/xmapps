<%= form_for(@ask_answer) do |f| %>
  <% if @ask_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ask_answer.errors.count, "error") %> prohibited this ask_answer from being saved:</h2>

      <ul>
      <% @ask_answer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
